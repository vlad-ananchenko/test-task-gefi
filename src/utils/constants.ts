import { join } from 'path';

export const ADDRESSES_FILE_PATH = join(
  __dirname,
  '../../src',
  'static',
  'addresses.json',
);

export const BALANCES_FILE_PATH = join(
  __dirname,
  '../../src',
  'static',
  'balances.json',
);
